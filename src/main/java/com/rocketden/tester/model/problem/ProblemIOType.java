package com.rocketden.tester.model.problem;

public enum ProblemIOType {
    STRING, INTEGER, DOUBLE, CHARACTER, BOOLEAN, ARRAY_STRING, ARRAY_INTEGER,
    ARRAY_DOUBLE, ARRAY_CHARACTER, ARRAY_BOOLEAN;
}
